/**
 * @fileOverview Defines constant values used throughout the application.
 * This includes keys for localStorage items.
 */

/**
 * Key for storing bookmarks in localStorage.
 */
export const LOCAL_STORAGE_BOOKMARKS_KEY = 'gitaInsightsBookmarks';

/**
 * Key for storing text size preference in localStorage.
 */
export const LOCAL_STORAGE_TEXT_SIZE_KEY = 'gitaInsightsTextSize';
